version: 1
frontend:
  phases:
    build:
      commands:
        - '# Add specific build commands here if any'
  artifacts:
  baseDirectory: '/'
  files:
    - '**/*'
    - '/well-known/apple-app-site-association.json'

  cache:
    paths: []
customRules: